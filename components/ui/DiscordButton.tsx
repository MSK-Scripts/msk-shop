/**
 * @deprecated In den modernisierten Sections wird der Discord-Button direkt
 * über `<Button variant="discord">` aus `@/components/ui/Button` gerendert
 * (siehe components/home/CTASection.tsx). Diese Datei kann gelöscht werden.
 */
export function DiscordButton() {
  return null
}
